{
  "name": "claudectrl",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/server",
    "apps/web",
    "packages/shared"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c cyan,magenta \"npm run dev --workspace=apps/server\" \"npm run dev --workspace=apps/web\"",
    "dev:server": "npm run dev --workspace=apps/server",
    "dev:web": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=apps/server && npm run build --workspace=apps/web",
    "test": "npm run test --workspace=apps/server",
    "setup": "node scripts/setup.mjs",
    "autostart:install": "node scripts/install-autostart.mjs install",
    "autostart:remove": "node scripts/install-autostart.mjs remove"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
